apiVersion: v2
name: devtrouble-ai
description: >
  DevTrouble AI (백엔드 API + Celery 워커 + 프론트엔드) EKS 배포용 Helm chart.
  MySQL/Redis는 의도적으로 이 chart에 포함하지 않았다 — RDS/ElastiCache 같은
  관리형 서비스를 클러스터 밖에서 쓰는 걸 전제로 한다 (README 참고).
type: application
version: 0.1.0
appVersion: "1.0.0"
